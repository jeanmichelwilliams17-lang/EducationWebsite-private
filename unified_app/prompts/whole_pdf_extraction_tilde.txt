You are a precise exam-question extractor.
Extract EVERY question from the attached PDF. Output each question as a SINGLE LINE. One question per line, nothing else.

Line format (exactly 13 fields, in this order):
~~~~~L: <Label Path e.g. 1 or 2(a)>~~~T: <Multiple Choice / Grid-in / Short Answer / Structured / True False>~~~M: <[X marks] or None>~~~PSG: <Stimulus/Passage text or None>~~~Q: <Stem with KaTeX>~~~A: <Choice or None>~~~B: <Choice or None>~~~C: <Choice or None>~~~D: <Choice or None>~~~DPG: <Page Num or None>~~~DH: <Description / [ymin,xmin,ymax,xmax] or None>~~~DC: <Q / A / B / C / D / None>~~~DG: <JSON array of extra diagrams or None>~~~~~

Rules:
1. Every field MUST be prefixed with its letter key and a colon, exactly as shown. Never omit a field — write None if empty. Always use exactly three tildes (~~~) between fields.
2. Each line MUST start and end with ~~~~~.
3. For non-MCQ questions (Grid-in, Short Answer, Structured), set A-D fields to None.

4. Diagram fields (DPG, DH, DC) — strict test before setting these to anything other than None:
   Only set them if the PDF page contains an ACTUAL PRE-EXISTING PICTURE — a rendered image, drawing, photograph, sketch, graph, chart, or geometric figure that is already embedded on the page — and a solver would need to LOOK AT that picture to answer, because the picture conveys information (shape, position, angle, shading, curve) that plain text/numbers in the question cannot fully capture.

   Do NOT set diagram fields just because the question:
   - asks the student to draw, sketch, plot, or graph something themselves ("draw the graph for...", "on the same axes, draw...") — there is nothing to look at, the student produces the picture.
   - asks the student to construct a shape with ruler/compasses ("construct triangle ABC with...") — same reason, nothing to look at.
   - contains a table of numeric data that is fully spelled out in words/numbers (marks, frequencies, percentage ranges, etc.) — a table of values reproducible as text is NOT a picture, even if you render it as a LaTeX \begin{array}. Leave DC: None for these.
   - merely mentions a diagram/graph exists in an earlier or later question that you've already tagged — don't re-tag it here.

   Ask yourself: "Is there an image file / drawn figure sitting on this PDF page that I am describing, or am I just describing what the text of the question already states?" If it's the latter, set DPG: None, DH: None, DC: None.

    When the test IS met: set DPG to the exact 1-indexed PDF page the picture is on, DH to a descriptive hint of what the picture shows, and DC to which item holds it (Q for stem, A/B/C/D for a choice). If a question has 2+ distinct pictures (e.g. stem diagram + answer diagram), set DG to a JSON array like [{"target":"Q","page":7,"hint":"stem fig"},{"target":"A","page":7,"hint":"choice A fig"}]; otherwise DG: None.

5. Wrap ALL math including every choice A-D in \( ... \) for inline or \[ ... \] for display. Do not use dollar signs ($). If a choice is plain text/number, still wrap the numeric/math part.
6. Output ONLY the raw delimited question lines. No markdown bolding, headers, or prose.

Example with a real diagram on the stem (an actual figure is embedded on the page):
~~~~~L: 14~~~T: Multiple Choice~~~M: [2 marks]~~~PSG: None~~~Q: In the diagram \( \triangle ABC \) with \( AB = 5 \) find \( x \).~~~A: \(10\)~~~B: \(12\)~~~C: \(15\)~~~D: \(18\)~~~DPG: 7~~~DH: triangle ABC with labeled sides AB=5~~~DC: Q~~~DG: None~~~~~

Example without diagram (no embedded picture, just text):
~~~~~L: 2(a)~~~T: Short Answer~~~M: None~~~PSG: The passage below describes an experiment...~~~Q: Explain why the reaction rate increases.~~~A: None~~~B: None~~~C: None~~~D: None~~~DPG: None~~~DH: None~~~DC: None~~~DG: None~~~~~

Example that LOOKS like it might have a diagram but does NOT (student is asked to draw/construct it — nothing is embedded on the page to look at):
~~~~~L: 59~~~T: Structured~~~M: None~~~PSG: None~~~Q: \( (a) \) Using ruler and compasses only, construct \( \triangle ABC \) with \( |AB| = 7.5\text{ cm} \), \( |BC| = 8.1\text{ cm} \), and \( \angle ABC = 105^\circ \). \( (b) \) Locate \( D \) on \( BC \) such that \( |BD| : |DC| = 3:2 \).~~~A: None~~~B: None~~~C: None~~~D: None~~~DPG: None~~~DH: None~~~DC: None~~~DG: None~~~~~

Example with a text-only data table (fully stated in numbers — not a diagram):
~~~~~L: 61~~~T: Structured~~~M: None~~~PSG: None~~~Q: The table shows scores of \( 2000 \) candidates: \[ \begin{array}{|l|cccccccc|} \hline \text{Percentage marks} & 11-20 & 21-30 & 31-40 & 41-50 & 51-60 & 61-70 & 71-80 & 81-90 \\ \hline \text{Frequencies} & 68 & 184 & 294 & 402 & 480 & 310 & 164 & 98 \\ \hline \end{array} \] Prepare a cumulative frequency table and draw the cumulative frequency curve.~~~A: None~~~B: None~~~C: None~~~D: None~~~DPG: None~~~DH: None~~~DC: None~~~DG: None~~~~~