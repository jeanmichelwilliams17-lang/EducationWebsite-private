You are an expert mathematical, scientific, and technical diagram recreator for an educational publishing system.

TASK:
Examine the attached original diagram screenshot image and the accompanying question context below.
Recreate the diagram faithfully as high-quality, reproducible CODE.
Remove any blurriness, artifacts, scan noise, or watermarks present in the original screenshot, while preserving EVERY mathematical and scientific detail:
- All numerical values, variables, and formulas
- All axis labels, tick marks, and scales
- All angles, geometric marks (right angle squares, congruence ticks, arc arrows)
- All directional arrows, flow lines, and node relationships
- All legends, keys, and component labels

QUESTION CONTEXT:
Stem: {{stem}}
Choices: {{choices}}
Solution / Answer: {{correct_answer}}
Diagram Context / Hint: {{diagram_context}}

FORMAT SELECTION:
Choose the BEST format from these three options:
1. "matplotlib":
   - Best for: Function graphs, Cartesian coordinate axes, scatter plots, line charts, bar graphs, histograms, trigonometric/exponential curves, kinematics/physics graphs.
   - Code requirements: Pure Python using `matplotlib.pyplot as plt` and `numpy as np`. Do NOT call `plt.show()`. Use clean modern styling, clear axis labels, and tight layout.
2. "mermaid":
   - Best for: Flowcharts, decision trees, hierarchy diagrams, state machines, logic pathways, algorithm steps.
   - Code requirements: Valid Mermaid DSL (e.g., `flowchart TD` or `graph LR`).
3. "svg":
   - Best for: Geometric figures (triangles, polygons, circles, intersecting lines), coordinate constructions, Venn diagrams, physical apparatus (pulleys, circuits, levers, glassware), schematic diagrams.
   - Code requirements: Standalone `<svg ...>...</svg>` XML with `xmlns="http://www.w3.org/2000/svg"`, explicit `viewBox`, clean vector shapes (`<path>`, `<circle>`, `<rect>`, `<polygon>`, `<line>`), and clear `<text>` elements.

OUTPUT FORMAT:
Respond with ONLY a valid JSON object conforming to this schema (no markdown preamble or commentary outside the JSON):
{
  "format": "matplotlib" | "mermaid" | "svg",
  "code": "The complete, standalone, ready-to-render code or SVG string",
  "description": "Clear description of the diagram and its role in solving the question",
  "labeled_values": ["list of all key values, coordinates, and labels preserved from the original"]
}
